
  # Fix app and webapp size

  This is a code bundle for Fix app and webapp size. The original project is available at https://www.figma.com/design/33YmvMwHhS7AZvLT6U2ugM/Fix-app-and-webapp-size.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  